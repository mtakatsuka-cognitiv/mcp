# ScyllaDB connection settings
# Host can be localhost, a specific IP, or host.docker.internal for Docker Desktop
SCYLLA_HOST=localhost
SCYLLA_PORT=9042

# Optional: Username and password (only needed if authentication is enabled)
# Leave blank if your ScyllaDB cluster doesn't use authentication
SCYLLA_USER=
SCYLLA_PASSWORD=

# Optional: Default keyspace (not required, but can be set for convenience)
# SCYLLA_KEYSPACE=
